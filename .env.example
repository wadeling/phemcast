# 腾讯云邮件服务配置示例文件
# 复制此文件为 .env 并填入你的真实配置

# =============================================================================
# 腾讯云邮件服务配置 (必填)
# =============================================================================

# 腾讯云API密钥 (必填)
# 获取地址: https://console.cloud.tencent.com/cam/capi
TENCENT_CLOUD_SECRET_ID=your_secret_id_here
TENCENT_CLOUD_SECRET_KEY=your_secret_key_here

# 腾讯云区域 (可选，默认为ap-guangzhou)
# 支持的区域:
#   ap-guangzhou     - 广州 (推荐，延迟最低)
#   ap-shanghai      - 上海
#   ap-beijing       - 北京
#   ap-hongkong      - 香港
#   ap-singapore     - 新加坡
#   ap-seoul         - 首尔
#   ap-tokyo         - 东京
#   ap-mumbai        - 孟买
#   eu-frankfurt     - 法兰克福
#   na-ashburn       - 弗吉尼亚
#   na-siliconvalley - 硅谷
TENCENT_CLOUD_REGION=ap-guangzhou

# 发件人邮箱 (必填，需要在腾讯云SES中验证)
# 支持自定义域名邮箱，如: noreply@yourcompany.com, info@yourdomain.com
TENCENT_FROM_EMAIL=your_verified_email@yourdomain.com

# 腾讯云邮件模板配置 (可选)
# 模板ID，如果设置了则使用模板方式发送邮件
# TENCENT_TEMPLATE_ID=34712

# 是否使用模板发送邮件 (true/false，默认为true)
# 如果设置为false，则直接发送HTML和文本内容
TENCENT_USE_TEMPLATE=true

# =============================================================================
# OpenAI配置 (必填)
# =============================================================================

# OpenAI API密钥 (必填)
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI模型 (可选，默认为gpt-4-turbo-preview)
OPENAI_MODEL=gpt-4-turbo-preview

# OpenAI API基础URL (可选，用于兼容的端点)
OPENAI_BASE_URL=https://api.openai.com/v1

# 每篇文章最大token数 (可选，默认为2000)
MAX_TOKENS_PER_ARTICLE=2000

# =============================================================================
# SMTP邮件服务配置 (可选，作为备选邮件服务)
# =============================================================================

# SMTP服务器地址
# SMTP_SERVER=smtp.gmail.com

# SMTP服务器端口
# SMTP_PORT=587

# SMTP用户名
# EMAIL_USERNAME=your_smtp_email@gmail.com

# SMTP密码或应用密码
# EMAIL_PASSWORD=your_smtp_app_password

# 发件人显示名称
# EMAIL_FROM_NAME=Industry News Agent

# =============================================================================
# 应用配置 (可选)
# =============================================================================

# 日志级别 (可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 日志文件路径 (可选，留空则只输出到控制台)
# LOG_FILE=logs/app.log

# 是否显示文件行数 (可选，true/false)
SHOW_FILE_LINE=true

# 是否显示函数名 (可选，true/false)
SHOW_FUNCTION=true

# 调试模式
DEBUG=True

# 服务器主机地址
HOST=0.0.0.0

# 域名
SERVER_URL=http://localhost

# 服务器端口
PORT=8000

# =============================================================================
# 网络爬虫配置 (可选)
# =============================================================================

# 请求间隔延迟 (秒)
REQUEST_DELAY=2.0

# 最大并发请求数
MAX_CONCURRENT_REQUESTS=5

# 请求超时时间 (秒)
TIMEOUT=30

# =============================================================================
# 缓存和报告配置 (可选)
# =============================================================================

# 缓存TTL (分钟)
CACHE_TTL_MINUTES=360

# 报告输出目录
OUTPUT_DIR=reports

# 最大报告大小 (MB)
MAX_REPORT_SIZE_MB=10

# =============================================================================
# 使用说明
# =============================================================================
# 1. 复制此文件为 .env: cp env.example .env
# 2. 编辑 .env 文件，填入你的真实配置
# 3. 确保腾讯云API密钥和发件人邮箱已正确配置
# 4. 运行配置验证: python validate_email_config.py
# 5. 测试邮件服务: python test_tencent_email.py
# 6. 开始使用: python -m src.web_interface
